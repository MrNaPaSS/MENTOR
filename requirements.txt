# NMNH — runtime dependencies (бот + бэкенд)
aiogram>=3.4
SQLAlchemy>=2.0
APScheduler>=3.10
python-dotenv>=1.0
aiohttp>=3.9
fastapi>=0.110
uvicorn>=0.29
# Без неё uvicorn отвечает 404 на любой апгрейд до WebSocket:
# каналы /ws, /ws/prices и /ws/scalping молча не работают.
websockets>=12.0
python-multipart>=0.0.9
certifi>=2024.0
cryptography>=42.0
