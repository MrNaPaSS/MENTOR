"""Адаптер Binance: торговый клиент и приватный поток.

Пятая биржа терминала и самая дешёвая из всех: её рыночная половина работает у
нас в бою с самого начала - с `backend/scalping/binance.py` идут скринер,
книга и свечи всем, у кого своя биржа не подключена. Оставались торговый
клиент и приватный поток, и оба устроены как у BingX: объём в монетах, ключ
потока отдельной ручкой.

Оговорка, которую надо помнить раньше кода: **Binance запрещает партнёрам
возвращать комиссию пользователям** и засчитывает только новых пользователей
без чужого реф-кода. Значит кешбэка ученику здесь не будет, и на витрине это
написано прямо (`core/venues.py`), а не выясняется после регистрации.
"""
