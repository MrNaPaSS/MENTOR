"""Адаптер BingX: торговый клиент и приватный поток.

Третья биржа терминала. Отличий от OKX два: объём считается в монетах, а не в
контрактах (слой перевода не нужен вовсе), и метка брокера идёт заголовком, а
не полем в каждой заявке. Разбор документации биржи -
`docs/integrations/bingx-api.md`, что из него строится - `docs/tz/bingx-tz.md`.
"""
