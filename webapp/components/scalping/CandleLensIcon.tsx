/**
 * Свеча под лупой: знак разбора свечи по объёму.
 *
 * Своей рукой, а не из набора: там есть свеча и есть лупа, но нет одного
 * знака. Составленный из двух - свеча с лупой в углу - на четырнадцати точках
 * читался как клякса: лупа выходила меньше фитиля.
 *
 * Поэтому лупа здесь главная и во весь знак, а свеча стоит внутри неё - ровно
 * то, что кнопка и делает: приближает одну свечу и показывает, из чего она
 * собрана. Линии рисуются цветом текста кнопки, поэтому знак сам меняется
 * вместе с ней - включённой, выключенной и погашенной.
 */
export default function CandleLensIcon({ className }: { className?: string }) {
  return (
    <svg
      viewBox="0 0 24 24"
      fill="none"
      stroke="currentColor"
      strokeLinecap="round"
      strokeLinejoin="round"
      className={className}
      aria-hidden="true"
    >
      {/* Стекло и ручка - толщиной как у соседних значков в панели. */}
      <circle cx="10" cy="10" r="8.5" strokeWidth="2" />
      <path d="M16.2 16.2 21.5 21.5" strokeWidth="2" />
      {/* Свеча внутри - линиями тоньше: под стеклом она мельче самого стекла,
          и равной толщиной обе фигуры слипались бы в пятно. */}
      <path d="M10 3.6V6.4" strokeWidth="1.5" />
      <path d="M10 13.6V16.4" strokeWidth="1.5" />
      <rect x="7.6" y="6.4" width="4.8" height="7.2" rx="1" strokeWidth="1.5" />
    </svg>
  );
}
