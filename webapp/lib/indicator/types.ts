// Общие типы индикатора.
//
// Свеча описана здесь, а не в конкретном движке: её используют и структурная
// часть, и график, и расчёт средних. Держать определение в одном из них значило
// бы связать их между собой без причины.

export type Candle = {
  time: number;
  open: number;
  high: number;
  low: number;
  close: number;
  volume: number;
};
